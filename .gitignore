# ── Python ──
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.eggs/
pip-wheel-metadata/

# ── 虚拟环境 ──
.venv/
venv/
env/
ENV/

# ── 测试与覆盖率 ──
.coverage
.coverage.*
htmlcov/
.tox/
.pytest_cache/
.cache/
nosetests.xml
coverage.xml
*.cover

# ── uv ──
# uv.lock 需要提交到仓库（锁定依赖版本），不忽略

# ── 环境变量与密钥 ──
.env
.env.local
.env.*.local
*.key
*.pem
secrets.yaml

# ── IDE ──
.idea/
.vscode/
.vs/
*.swp
*.swo
*~
.DS_Store

# ── 数据库与存储 ──
*.sqlite
*.sqlite3
*.db
pgdata/
milvus_data/
redis_data/
minio_data/

# ── 日志 ──
logs/
*.log

# ── 文档构建 ──
site/
.trae/
.workbuddy/
# ── 临时文件 ──
*.tmp
*.bak
tmp/

# ── 评测与报告（指标报告为面试证据, 必须入库, 不忽略）──
# 由 scripts/gen_openapi.py 生成, 不入库
openapi.json
