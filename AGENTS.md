# AGENTS.md — KnowFlow AI 开发规范

> 本文件是 AI 参与 KnowFlow 项目开发时的行为准则。开发前必读。
> 配套文档：《KnowFlow 项目设计文档》（做什么）《KnowFlow 项目结构》（目录）《KnowFlow 开发计划》（顺序与验收）

---

## 〇、项目速览

- **KnowFlow**：企业知识库 Agent 平台，可编排、可扩展，用于 Agent 开发岗位面试。
- **技术栈**：Python 3.13 + uv ｜ FastAPI ｜ LangGraph / LangChain ｜ MCP ｜ Milvus ｜ PostgreSQL ｜ MinIO ｜ Redis
- **开发节奏**：按《开发计划》P0 → P11 顺序推进，每个 Phase 有任务清单与验收标准，验收通过才进入下一 Phase。
- **核心红线**：量化指标（8% / 34.2% / 32.6% / 94+% / 77.6%）必须实测可复现，**严禁伪造数据**。
- **运行环境**：Windows + PowerShell。命令分隔用 `;`，**不要使用 `&&`**。

---

## 一、开发准则

### 1.1 动手前先想清楚

- **假设必须明示**：开始实现前，把假设说出来；不确定就问用户，不要默默选一个。
- **矛盾先指出**：发现设计文档、项目结构、开发计划之间存在冲突或缺口时（例如 checkpoint 存 Redis 还是 PG），先向用户说明并确认决策，再动手写代码。
- **多方案呈现取舍**：一个需求有多种实现路径时，列出选项与取舍，不自行拍板。
- **敢于推回去**：如果存在更简单的方案，直接说出来。设计文档写"复杂"不等于必须实现得复杂。

### 1.2 简单优先

- 只实现被要求的功能，不添加任何投机性的东西。
- 不为单次使用的代码建抽象层，不为没提出的需求加"灵活性/可配置性"。
- 不为不可能发生的场景写错误处理。
- 如果写出来 200 行、而 50 行能解决，重写。
- 遵循开发计划 1.3 的"先跑通再优化"：先最小可用链路，再补细节（如 reranker 先接简单的，再换 cross-encoder）。

### 1.3 外科手术式修改

- 只改必须改的文件与代码行，每一处改动都应能追溯到用户本次请求。
- 不顺手"优化"相邻代码、注释或格式；不重构没坏的东西。
- 风格跟随项目现有代码（即使个人偏好不同）。
- 发现无关的遗留死代码：**提出来，不删除**。
- 自己改动造成孤儿（如不再使用的 import / 变量 / 函数）：必须清掉；但不要清理用户原有的死代码。

### 1.4 目标驱动执行

- 把任务转化为可验证目标，例如：
  - "实现混合检索" → 验证：`uv run pytest tests/unit/retrieval -q` 全绿 + `compare_baseline.py` 输出对比报告
  - "修复某 bug" → 验证：先写复现测试，再让它通过
- 多步骤任务先给出简短计划：`1. 步骤 → 验证: 检查 2. 步骤 → 验证: 检查`
- 完成标准 = 开发计划中该 Phase 的验收标准 + 第二节门禁全绿，缺一不可。

---

## 二、工程门禁（每次代码变更后必须全绿）

```bash
uv run ruff check src/ tests/ scripts/ worker/          # 0 errors
uv run ruff format --check src/ tests/ scripts/ worker/ # 格式通过
uv run mypy src/ worker/                                # 0 errors
uv run pytest tests/unit -q                             # 全绿
```

- 门禁通过后执行 `uv run pre-commit run --all-files`（若已安装）。
- 新增/修改功能必须配套测试：核心算法（分块/融合/扩展/精排/域隔离/卸载/并发）进 `tests/unit/`；跨模块链路进 `tests/integration/`。
- 覆盖率目标：核心模块 ≥ 70%，整体 ≥ 60%。

### 2.2 量化指标测试文档

- 量化指标的测量若需要项目运行起来才能执行（依赖真实服务、真实模型、外部依赖或手工场景），且当前环境无法完整执行时，AI 编写一份**测试文档**交由用户实测，**禁止代为填写结果**。
- 测试文档写入 `docs/tests/`（如 `docs/tests/指标测试-工具治理.md`），必须包含：前置条件（依赖服务/环境变量）、启动步骤、逐条测试步骤与命令、每条预期结果、结果记录表（留空待用户填写）。
- 用户按文档测试后，将输出/截图/结果表反馈给 AI；AI 将实测数据写入对应指标报告（`eval/reports/` 或 `docs/benchmarks/`），并保留测试文档作为证据。
- 能在当前环境自动验证的指标（单测可覆盖、脚本可运行）直接自动测量，不走此流程。

---

## 三、Git / GitHub 提交规范

### 3.1 提交时机（最重要的一条）

> **除非用户明确要求（如“提交一下”“commit”），否则绝不执行 `git commit` / `git push` / `git add` 之外的任何写操作。**
> 用户没要求时，完成代码修改后正常汇报，并在结尾说明“本次变更未提交，如需提交请告知”。
> 用户要求提交时，**必须先按 3.6 更新 `docs/commit-log.md`，再执行 git commit**。

### 3.2 提交前的检查清单

- [ ] 第二节门禁全绿，相关测试通过
- [ ] 只暂存本次任务相关的文件（`git add` 精确到文件路径，不默认 `git add -A`）
- [ ] 确认没有敏感信息：`.env`、API Key、密钥、本地路径、个人信息
- [ ] 确认没有未完成任务塞进同一个提交

### 3.3 commit message 格式（Conventional Commits，中文）

```
<type>(<scope>): <subject>

<body（可选，说明为什么改、怎么改）>
```

| 项 | 规则 |
|---|---|
| type | `feat` 新功能 / `fix` 修复 / `docs` 文档 / `style` 格式 / `refactor` 重构 / `perf` 性能 / `test` 测试 / `build` 构建 / `ci` CI / `chore` 杂项 / `revert` 回滚 |
| scope | 模块名：`retrieval` `tools` `context` `agents` `sandbox` `memory` `observability` `api` `services` `db` `core` `worker` `eval` `docs` 等；跨多模块时可省略 |
| subject | **中文**，祈使句动词开头（新增/修复/重构/补充/移除…），≤ 50 字，结尾不加句号 |
| body | 可选；说明"为什么改"和"怎么改"，多行用 `- ` 列表 |

### 3.4 示例

```
feat(retrieval): 新增混合检索 RRF 融合

- 向量召回与 BM25 双路召回后按 RRF 融合
- 融合参数 k 默认 60，纳入配置
- 补充融合边界单测

fix(tools): 修复 Skill 依赖解析循环检测缺失

test(context): 补充卸载阈值与引用替换单测

docs(adr): 记录 checkpoint 存储决策

chore: 升级 langgraph 至 0.2.40
```

### 3.5 提交粒度

- **一个提交只做一件事**：新功能、修复、测试、文档分开提交。
- 逻辑强相关的小改动（如同一功能的实现+测试）可合并为一个提交。
- 不提交未验证代码、不提交中间态、不提交调试残留（print/临时文件）。

### 3.6 提交日志（commit-log.md）

- **用户要求提交时，先把本次所有 commit 写入 `docs/commit-log.md`，再按日志顺序执行 git commit。**
- 时间线规则：
  - 时间线起点：**2026 年 6 月初**，首条记录从 2026-06-01 开始。
  - 若 `docs/commit-log.md` 已有记录，本次 commit 的日期必须**晚于最后一条记录的日期**（向后推迟：通常从最后日期 +1 天起，同一批次多条可分布在相邻日期，按提交顺序递增）。
- 记录格式：`YYYY-MM-DD | <type>(<scope>): <subject>`
- 执行 git commit 时，用日志中的日期设置提交时间（PowerShell）：
  ```powershell
  $env:GIT_AUTHOR_DATE="2026-06-01T10:00:00"
  $env:GIT_COMMITTER_DATE="2026-06-01T10:00:00"
  git commit -m "..."
  ```
- `docs/commit-log.md` 的更新本身作为批次中最后一个 `docs: 更新提交日志` 提交，且该提交不写入日志（避免自引用）。

---

## 四、日常开发工作流

```
1. 确认当前处于哪个 Phase（《开发计划》），阅读该 Phase 任务清单与对应设计文档章节
2. 按《项目结构》的目录落文件，实现代码
3. 执行第二节门禁命令，全绿
4. 编写/补充该阶段测试，pytest 全绿
5. 执行该 Phase 验收标准（命令或手工验证）；需运行环境实测的指标先写 `docs/tests/` 测试文档交用户实测，结果回传后写入 eval/reports/ 或 docs/benchmarks/
6. 向用户汇报：做了什么、验证结果、未提交提醒
```

---

## 五、红线清单

- 不主动 commit / push（除非用户明确要求）。
- 不伪造、不虚报量化指标；指标达不到设计目标时如实报告，说明实测值与差距。
- 不提交 `.env`、密钥、隐私数据。
- 不删除未要求的文件；删除任何文件前先说明。
- 不修改设计文档/结构文档/开发计划中的既定决策，除非用户要求（新决策走 `docs/adr/`）。
- 遇到文档与代码不一致时：先指出、再修改，不在沉默中自行选边。
- 提交必须先更新 `docs/commit-log.md` 且时间线单调递增，不与已有记录重叠。
